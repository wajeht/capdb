{
	"name": "capdb",
	"version": "0.0.0",
	"description": "database management cli for docker environments",
	"type": "module",
	"main": "./src/index.js",
	"bin": {
		"capdb": "./src/index.js"
	},
	"scripts": {
		"prepare": "husky install",
		"cli": "npm unlink capdb && chmod +x ./src/index.js && npm link && rm -f ./src/database/db.sqlite && npm run migrate:latest",
		"format:check": "prettier --check .",
		"format": "prettier --write .",
		"lint:check": "eslint .",
		"lint": "eslint --fix .",
		"test:watch": "vitest",
		"test": "vitest --run",
		"rollback": "knex migrate:rollback --knexfile=./src/database/knexfile.js",
		"migrate:make": "knex migrate:make --knexfile=./src/database/knexfile.js",
		"migrate:latest": "knex migrate:latest --knexfile=./src/database/knexfile.js",
		"migrate:rollback": "knex migrate:rollback --knexfile=./src/database/knexfile.js",
		"seed:make": "knex seed:make --knexfile=./src/database/knexfile.js",
		"seed:run": "knex seed:run --knexfile=./src/database/knexfile.js",
		"setup": "if ! command -v pm2 &> /dev/null; then echo 'Installing PM2...'; npm install pm2 -g; fi",
		"clean": "if pm2 show capdb &> /dev/null; then echo 'Stopping and deleting existing capdb...'; pm2 stop capdb; pm2 delete capdb; fi",
		"install-deps": "npm install",
		"start-pm2": "npm run clean && npm run install-deps && pm2 start ./src/commands/start.js --name capdb && pm2 startup && pm2 save --force",
		"start": "npm run setup && npm run start-pm2",
		"backup": "echo '' && echo 'backup scheduler has been started' && echo '' && exit 0"
	},
	"keywords": [
		"postgres",
		"mysql"
	],
	"repository": {
		"type": "git",
		"url": "https://github.com/wajeht/capdb.git"
	},
	"bugs": {
		"url": "https://github.com/wajeht/capdb/issues"
	},
	"publishConfig": {
		"registry": "https://npm.pkg.github.com/",
		"access": "public"
	},
	"homepage": "https://github.com/wajeht/capdb#readme",
	"author": "wajeht",
	"license": "MIT",
	"devDependencies": {
		"@commitlint/cli": "^17.7.2",
		"@commitlint/config-conventional": "^17.7.0",
		"eslint": "^8.51.0",
		"eslint-config-prettier": "^9.0.0",
		"eslint-plugin-prettier": "^5.0.0",
		"husky": "^8.0.3",
		"prettier": "^3.0.3",
		"vitest": "^0.34.6"
	},
	"engines": {
		"node": ">=20"
	},
	"dependencies": {
		"@inquirer/prompts": "^3.2.0",
		"commander": "^11.0.0",
		"dockerode": "^3.3.5",
		"fastq": "^1.15.0",
		"knex": "^2.5.1",
		"node-cron": "^3.0.2",
		"pm2": "^5.3.0",
		"sqlite3": "^5.1.6"
	}
}
